<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tv_column_name"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="头条"
        android:gravity="center"
        android:background="@drawable/shap_edit_text_bac"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"/>

    <ImageView
        android:id="@+id/iv_remove"
        android:layout_width="16dp"
        android:layout_height="16dp"
        app:layout_constraintLeft_toLeftOf="@id/tv_column_name"
        app:layout_constraintRight_toLeftOf="@id/tv_column_name"
        app:layout_constraintTop_toTopOf="@id/tv_column_name"
        app:layout_constraintBottom_toTopOf="@id/tv_column_name"
        android:src="@drawable/custom_column_cancel"/>

</android.support.constraint.ConstraintLayout>